// Basic configuration for Designap shop flow.
// Isi `slug` dengan slug proyek Pakasir kamu (lihat dashboard Pakasir > Proyek).
// Catatan: integrasi via URL TIDAK membutuhkan API Key.

window.DESIGNAP_CONFIG = {
  pakasir: {
    slug: "DesainAP"
  }
};
